version: '3.8'

services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_DB: vividus
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
    # Порт доступен только на localhost (для SSH туннеля через DBeaver)
    ports:
      - "127.0.0.1:5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./src/database/migrations:/docker-entrypoint-initdb.d
    command: ["postgres", "-c", "log_statement=all"]

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

  app:
    build: .
    depends_on:
      - postgres
      - redis
    env_file:
      - .env
    environment:
      - DB_HOST=postgres
      - REDIS_HOST=redis
      - PORT=3000
    volumes:
      - ./uploads:/app/uploads
    ports:
      - "3000:3000"

  admin-panel:
    build: .
    depends_on:
      - postgres
    env_file:
      - .env
    environment:
      - DB_HOST=postgres
      - ADMIN_PORT=3001
    command: npm run start:admin
    volumes:
      - ./src/admin/public:/app/dist/admin/public

  nginx:
    image: nginx:alpine
    depends_on:
      - admin-panel
    ports:
      - "80:80"
    volumes:
      - ./nginx-admin.conf:/etc/nginx/conf.d/default.conf

volumes:
  postgres_data:
  redis_data:
